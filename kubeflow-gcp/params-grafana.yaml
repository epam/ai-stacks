parameters:
- name: component
  component: grafana
  parameters:
  - name: grafana.namespace
    value: kube-system
  - name: grafana.admin.user
    fromEnv: USER
  - name: grafana.admin.password
    fromEnv: DEX_PASSWORD
  - name: ingress.host
    value: grafana.${dns.domain}
